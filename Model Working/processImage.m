%TODO: Apply same preprocessing and segmentation as in the training images
%Resize to same size of training images.
function imageOut = processImage(imageIn)
    %Add code here for all preprocessign and segmentation, return
    %processed and segmented image as 'imageOut'
    imageOut = imageIn;
end